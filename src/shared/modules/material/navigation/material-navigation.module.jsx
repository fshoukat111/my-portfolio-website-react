import * as MaterialUI from '@mui/material';


/**
 * reusable Material Drawer with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Drawer = (props) => {
    return (
        <MaterialUI.Drawer {...props} />
    )
}

/**
 * reusable Material Link with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Link = (props) => {
    return (
        <MaterialUI.Link {...props} />
    )
}

/**
 * reusable Material MenuList with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const MenuList = (props) => {
    return (
        <MaterialUI.MenuList {...props} />
    )
}

/**
 * reusable Material MenuItem  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const MenuItem = (props) => {
    return (
        <MaterialUI.MenuItem {...props} />
    )
}

/**
 * reusable Material Pagination  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Pagination = (props) => {
    return (
        <MaterialUI.Pagination {...props} />
    )
}

/**
 * reusable Material Stepper with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Stepper = (props) => {
    return (
        <MaterialUI.Stepper  {...props} />
    )
}

/**
 * reusable Material Step with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Step = (props) => {
    return (
        <MaterialUI.Step  {...props} />
    )
}

/**
 * reusable Material StepLabel with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const StepLabel = (props) => {
    return (
        <MaterialUI.StepLabel  {...props} />
    )
}

/**
 * reusable Material Tabs with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Tabs = (props) => {
    return (
        <MaterialUI.Tabs {...props} />
    )
}

/**
 * reusable Material Tab with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Tab = (props) => {
    return (
        <MaterialUI.Tab {...props} />
    )
}

/**
 * reusable Material SpeedDial with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const SpeedDial = (props) => {
    return (
        <MaterialUI.SpeedDial {...props} />
    )
}