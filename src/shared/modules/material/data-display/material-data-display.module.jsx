import * as MaterialUI from '@mui/material';

/**
 * reusable Material Avatar with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Avatar = (props) => {
    return (
        <MaterialUI.Avatar {...props} />
    )
}

/**
 * reusable Material Badge with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Badge = (props) => {
    return (
        <MaterialUI.Badge {...props} />
    )
}

/**
 * reusable Material Chip with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Chip = (props) => {
    return (
        <MaterialUI.Chip {...props} />
    )
}

/**
 * reusable Material Divider with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Divider = (props) => {
    return (
        <MaterialUI.Divider {...props} />
    )
}

/**
 * reusable Material List with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const List = (props) => {
    return (
        <MaterialUI.List {...props} />
    )
}

/**
 * reusable Material Table with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Table = (props) => {
    return (
        <MaterialUI.Table {...props} />
    )
}

/**
 * reusable Material Tooltip with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Tooltip = (props) => {
    return (
        <MaterialUI.Tooltip {...props} />
    )
}

/**
 * reusable Material Typography with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Typography = (props) => {
    return (
        <MaterialUI.Typography {...props} />
    )
}

/**
 * reusable Material ListItem with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const ListItem = (props) => {
    return (
        <MaterialUI.ListItem {...props} />
    )
}

/**
 * reusable Material ListItemText with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const ListItemText = (props) => {
    return (
        <MaterialUI.ListItemText {...props} />
    )
}