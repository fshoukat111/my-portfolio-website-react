import * as MaterialUI from '@mui/material';

/**
 * reusable Material Box with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Box = (props) => {
    return (
        <MaterialUI.Box {...props} />
    )
}

/**
 * reusable Material Container with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Container = (props) => {
    return (
        <MaterialUI.Container {...props} />
    )
}

/**
 * reusable Material Grid with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Grid = (props) => {
    return (
        <MaterialUI.Grid {...props} />
    )
}

/**
 * reusable Material Toolbar with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Toolbar = (props) => {
    return (
        <MaterialUI.Toolbar {...props} />
    )

}

/**
 * reusable Material Stack  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Stack = (props) => {
    return (
        <MaterialUI.Stack  {...props} />
    )

}