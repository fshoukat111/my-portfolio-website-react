import * as MaterialUI from "@mui/material";

/**
 * reusable Material Autocomplete with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Autocomplete = (props) => {
  return <MaterialUI.Autocomplete {...props} />;
};

/**
 * reusable Material Button with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Button = (props) => {
  return <MaterialUI.Button {...props} />;
};

/**
 * reusable Material ButtonGroup  with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const ButtonGroup = (props) => {
  return <MaterialUI.ButtonGroup {...props} />;
};

/**
 * reusable Material Checkbox with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Checkbox = (props) => {
  return <MaterialUI.Checkbox {...props} />;
};

/**
 * reusable Material Radio with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Radio = (props) => {
  return <MaterialUI.Radio {...props} />;
};

/**
 * reusable Material Rating  with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Rating = (props) => {
  return <MaterialUI.Rating {...props} />;
};

/**
 * reusable Material Select with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Select = (props) => {
  return <MaterialUI.Select {...props} />;
};

/**
 * reusable Material Slider with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Slider = (props) => {
  return <MaterialUI.Slider {...props} />;
};

/**
 * reusable Material Switch with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Switch = (props) => {
  return <MaterialUI.Switch {...props} />;
};

/**
 * reusable Material TextField with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const TextField = (props) => {
  return <MaterialUI.TextField {...props} />;
};

/**
 * reusable Material ToggleButton  with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const ToggleButton = (props) => {
  return <MaterialUI.ToggleButton {...props} />;
};

/**
 * reusable Material Fab  with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Fab = (props) => {
  return <MaterialUI.Fab {...props} />;
};

/**
 * reusable Material FormControlLabel  with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const FormControlLabel = (props) => {
  return <MaterialUI.FormControlLabel {...props} />;
};

export const FormControl = (props)=>{
  return <MaterialUI.FormControl {...props}/>
}

/**
 * reusable Material Input with just export with diffrent style
 * @param {*} props
 * @returns
 */
export const Input = (props) => {
  return <MaterialUI.Input {...props} />;
};

export const Label = (props) => {
  return <MaterialUI.InputLabel {...props} />;
};

export const InputField= (props)=>{
    return <MaterialUI.TextField {...props} />
}

export const IconButton = (props)=>{
    return <MaterialUI.IconButton  {...props} />
}

export const NativeSelect  = (props)=>{
    return <MaterialUI.NativeSelect   {...props} />
}

export const Skeleton  = (props)=>{
    return <MaterialUI.Skeleton   {...props} />
}
